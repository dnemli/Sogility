/** Must match `scaleToCanvas` in `dashboard-from-training.ts` and `ClusterMap` viewBox. */
export const ARCHETYPE_CHART_WIDTH = 700;
/** Taller canvas so quadrant captions sit in equal top/bottom margins outside the plot. */
export const ARCHETYPE_CHART_HEIGHT = 560;
export const ARCHETYPE_CHART_PAD = 52;
